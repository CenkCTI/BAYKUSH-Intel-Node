import { describe,expect,it } from "vitest";
import { computeAggregateValue,computeDistributionValues,validZeroAllowed } from "../src/measurement/aggregation/math.js";
describe("NODE-3 aggregation semantics",()=>{it("does not sum child-bucket distinct counts",()=>{const facts=[{numericValue:1,entityKey:"ioc:a",dimensions:{}},{numericValue:1,entityKey:"ioc:a",dimensions:{}},{numericValue:1,entityKey:"ioc:b",dimensions:{}}];expect(computeAggregateValue("COUNT_DISTINCT",facts)).toBe(2);});it("builds deterministic top-N distributions with OTHER",()=>{const values=computeDistributionValues([{key:"IOC_TYPE",topN:2,includeOther:true}],[{numericValue:1,entityKey:null,dimensions:{IOC_TYPE:"DOMAIN"}},{numericValue:1,entityKey:null,dimensions:{IOC_TYPE:"IP"}},{numericValue:1,entityKey:null,dimensions:{IOC_TYPE:"URL"}},{numericValue:1,entityKey:null,dimensions:{IOC_TYPE:"IP"}}]);expect(values.map((value)=>[value.dimensionValue,value.count])).toEqual([["IP",2],["DOMAIN",1],["OTHER",1]]);});it("permits zero only under measurement-specific valid coverage",()=>{expect(validZeroAllowed({coveragePolicy:"SOURCE_COVERAGE_REQUIRED",liveCoverage:"COMPLETE",dataAvailability:"UNAVAILABLE"})).toBe(true);expect(validZeroAllowed({coveragePolicy:"SOURCE_COVERAGE_REQUIRED",liveCoverage:"NO_COVERAGE",dataAvailability:"UNAVAILABLE"})).toBe(false);expect(validZeroAllowed({coveragePolicy:"SOURCE_TIME_AVAILABILITY_REQUIRED",liveCoverage:"NO_COVERAGE",dataAvailability:"AVAILABLE"})).toBe(true);expect(validZeroAllowed({coveragePolicy:"SOURCE_TIME_AVAILABILITY_REQUIRED",liveCoverage:"COMPLETE",dataAvailability:"PARTIAL"})).toBe(false);});});
