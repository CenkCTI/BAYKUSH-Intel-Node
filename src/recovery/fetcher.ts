import { createHash, randomUUID } from "node:crypto";
import { createWriteStream } from "node:fs";
import { mkdir, rm, statfs } from "node:fs/promises";
import { once } from "node:events";
import path from "node:path";
import { config } from "../config.js";
import { RecoveryFailure } from "./errors.js";

const HOST="data.ris.ripe.net";
const PATH=/^\/rrc\d{2}\/\d{4}\.\d{2}\/update\.\d{8}\.\d{4}\.gz$/;
export function assertOfficialRipeMrtUrl(value:string):URL{let url:URL;try{url=new URL(value);}catch(error){throw new RecoveryFailure("DOWNLOAD_REDIRECT_REJECTED","Invalid RIPE MRT URL",error);}if(url.protocol!=="https:"||url.hostname!==HOST||url.port!==""||url.username||url.password||url.search||url.hash||!PATH.test(url.pathname))throw new RecoveryFailure("DOWNLOAD_REDIRECT_REJECTED","Recovery URL is outside the fixed RIPE MRT allowlist");return url;}

async function fetchBounded(url:URL):Promise<Response>{let current=url;for(let redirects=0;redirects<=2;redirects+=1){let response:Response;try{response=await fetch(current,{redirect:"manual",signal:AbortSignal.timeout(config.recoveryDownloadTimeoutMs),headers:{"user-agent":"BAYKUSH-Intel-Node/NODE-6.2"}});}catch(error){const name=error instanceof Error?error.name:"";throw new RecoveryFailure(name.includes("Timeout")?"DOWNLOAD_TIMEOUT":"DOWNLOAD_TLS_ERROR","RIPE MRT download failed",error);}if([301,302,303,307,308].includes(response.status)){const location=response.headers.get("location");if(!location)throw new RecoveryFailure("DOWNLOAD_REDIRECT_REJECTED","RIPE redirect omitted Location");const next=assertOfficialRipeMrtUrl(new URL(location,current).toString());if(next.hostname!==current.hostname)throw new RecoveryFailure("DOWNLOAD_REDIRECT_REJECTED","Cross-host MRT redirect rejected");current=next;continue;}return response;}throw new RecoveryFailure("DOWNLOAD_REDIRECT_REJECTED","Too many MRT redirects");}

export interface DownloadedMrtArtifact{sourceUrl:string;sha256:string;compressedBytes:number;httpStatus:number;etag:string|null;lastModified:string|null;stagingKey:string;absolutePath:string;downloadedAt:string;expiresAt:string;}
export async function downloadRipeMrtArtifact(input:{segmentId:string;sourceUrl:string;windowEnd:string}):Promise<DownloadedMrtArtifact>{if(!/^[0-9a-f-]{36}$/i.test(input.segmentId))throw new RecoveryFailure("DOWNLOAD_REDIRECT_REJECTED","Invalid recovery segment identity");const url=assertOfficialRipeMrtUrl(input.sourceUrl);await mkdir(config.recoveryStagingDir,{recursive:true,mode:0o700});const fs=await statfs(config.recoveryStagingDir);const available=Number(fs.bavail)*Number(fs.bsize);if(available<config.recoveryMinFreeDiskBytes)throw new RecoveryFailure("DISK_WATERMARK","Recovery staging disk watermark reached");const response=await fetchBounded(url);if(response.status===404){const settleUntil=Date.parse(input.windowEnd)+config.recoveryArchiveSettleSeconds*1000;throw new RecoveryFailure(Date.now()<settleUntil?"ARCHIVE_NOT_READY":"HTTP_NOT_FOUND",`RIPE MRT artifact returned HTTP 404: ${url.pathname}`);}if(response.status===429)throw new RecoveryFailure("HTTP_RATE_LIMITED","RIPE MRT archive rate limited recovery");if(response.status>=500)throw new RecoveryFailure("HTTP_SERVER_ERROR",`RIPE MRT archive returned HTTP ${response.status}`);if(!response.ok||!response.body)throw new RecoveryFailure("HTTP_NOT_FOUND",`RIPE MRT archive returned HTTP ${response.status}`);const contentLength=Number(response.headers.get("content-length")??"0");if(Number.isFinite(contentLength)&&contentLength>config.recoveryMaxArtifactBytes)throw new RecoveryFailure("DOWNLOAD_SIZE_LIMIT","RIPE MRT artifact exceeds compressed-byte limit");const dir=path.join(config.recoveryStagingDir,input.segmentId);await mkdir(dir,{recursive:true,mode:0o700});const name=`${randomUUID()}.mrt.gz`;const absolutePath=path.join(dir,name);const stagingKey=`${input.segmentId}/${name}`;const writer=createWriteStream(absolutePath,{flags:"wx",mode:0o600});const hash=createHash("sha256");let bytes=0;try{const reader=response.body.getReader();for(;;){const item=await reader.read();if(item.done)break;bytes+=item.value.byteLength;if(bytes>config.recoveryMaxArtifactBytes){await reader.cancel();throw new RecoveryFailure("DOWNLOAD_SIZE_LIMIT","RIPE MRT artifact exceeded compressed-byte limit while streaming");}hash.update(item.value);if(!writer.write(item.value))await once(writer,"drain");}writer.end();await once(writer,"close");}catch(error){writer.destroy();await rm(absolutePath,{force:true});if(error instanceof RecoveryFailure)throw error;throw new RecoveryFailure("DOWNLOAD_TLS_ERROR","RIPE MRT streaming download failed",error);}if(bytes===0){await rm(absolutePath,{force:true});throw new RecoveryFailure("GZIP_INVALID","Downloaded RIPE MRT artifact is empty");}const downloadedAt=new Date();return{sourceUrl:url.toString(),sha256:hash.digest("hex"),compressedBytes:bytes,httpStatus:response.status,etag:response.headers.get("etag"),lastModified:response.headers.get("last-modified"),stagingKey,absolutePath,downloadedAt:downloadedAt.toISOString(),expiresAt:new Date(downloadedAt.getTime()+config.recoveryArtifactRetentionHours*3600_000).toISOString()};}
