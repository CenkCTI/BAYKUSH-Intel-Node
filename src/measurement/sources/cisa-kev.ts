import type { MeasurementSourceProjector } from "../projection/types.js";
import { acquisitionBasis,factMap,liveObservationAllowed,makeCandidate,objectPayload,projectDefaultEntityObservations,stringFact } from "../projection/utils.js";
export const cisaKevMeasurementProjector:MeasurementSourceProjector={sourceKey:"CISA_KEV",projectCanonical(input,measurementKey){const facts=factMap(input.facts);const rawBasis=acquisitionBasis(input.trigger,input.purpose);const basis=rawBasis==="INITIAL_BOOTSTRAP"?"SNAPSHOT_RECONSTRUCTION":rawBasis;if(measurementKey==="vulnerability.cisa_kev.additions"){const dateAdded=stringFact(facts,"kev.date_added");const cve=stringFact(facts,"kev.cve_id");if(!dateAdded||!cve)return[];return[makeCandidate({measurementKey,identity:{cve},factKind:"EVENT",eventTime:null,eventDate:dateAdded,timePrecision:"DATE",numericValue:1,entityKey:`cve:${cve}`,entityType:"CVE",dimensions:{},acquisitionBasis:basis,sourceModelVersion:null,inputRole:"CANONICAL_RECORD",fingerprintMaterial:input.normalizedSha256})];}if(measurementKey==="vulnerability.cisa_kev.updates_observed"){if(input.sourceRevisionNumber<=1||!liveObservationAllowed(input))return[];return[makeCandidate({measurementKey,identity:{sourceRecordId:input.sourceRecordId,rawRecordId:input.rawRecordId},factKind:"OBSERVATION",eventTime:input.receivedAt,eventDate:null,timePrecision:"INSTANT",numericValue:1,entityKey:input.canonicalKey,entityType:"CVE",dimensions:{},acquisitionBasis:rawBasis,sourceModelVersion:null,inputRole:"CANONICAL_RECORD",fingerprintMaterial:input.normalizedSha256})];}return[];},projectRaw(input,measurementKey){if(input.sourceRecordId!=="__catalog_manifest__")return[];const payload=objectPayload(input.payload);if(!payload)return[];const basis=acquisitionBasis(input.trigger,input.purpose);if(measurementKey==="vulnerability.cisa_kev.catalog_size"){const count=typeof payload.count==="number"&&Number.isInteger(payload.count)?payload.count:null;if(count===null)return[];return[makeCandidate({measurementKey,identity:{payloadSha256:input.payloadSha256},factKind:"STATE_VALUE",eventTime:input.receivedAt,eventDate:null,timePrecision:"INSTANT",numericValue:count,entityKey:null,entityType:null,dimensions:{},acquisitionBasis:basis,sourceModelVersion:typeof payload.catalogVersion==="string"?payload.catalogVersion:null,inputRole:"RAW_MANIFEST",fingerprintMaterial:input.payloadSha256})];}if(measurementKey==="vulnerability.cisa_kev.membership_removals_observed"){if(!input.previousRawRecord||!liveObservationAllowed(input))return[];const previous=objectPayload(input.previousRawRecord.payload);const currentIds=Array.isArray(payload.cveIds)?payload.cveIds.filter((value):value is string=>typeof value==="string"):[];const previousIds=Array.isArray(previous?.cveIds)?previous.cveIds.filter((value):value is string=>typeof value==="string"):[];const currentSet=new Set(currentIds);return previousIds.filter((cve)=>!currentSet.has(cve)).sort().map((cve)=>makeCandidate({measurementKey,identity:{previous:input.previousRawRecord?.payloadSha256,current:input.payloadSha256,cve},factKind:"OBSERVATION",eventTime:input.receivedAt,eventDate:null,timePrecision:"INSTANT",numericValue:1,entityKey:`cve:${cve}`,entityType:"CVE",dimensions:{},acquisitionBasis:basis,sourceModelVersion:typeof payload.catalogVersion==="string"?payload.catalogVersion:null,inputRole:"RAW_MANIFEST_PAIR",fingerprintMaterial:[input.previousRawRecord?.payloadSha256,input.payloadSha256]}));}return[];},projectEntityObservations:projectDefaultEntityObservations};
