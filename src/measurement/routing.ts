import{createHash}from"node:crypto";import type{PoolClient}from"pg";import{withTransaction}from"../db/pool.js";import{canonicalJsonStringify}from"../runtime/raw-record.js";import{bucketForInstant}from"./time.js";import{getMeasurementRegistration}from"./registry.js";import type{MeasurementGranularity}from"./contracts.js";
const KEYS=["routing.ripe_ris.update_messages","routing.ripe_ris.announcement_prefix_events","routing.ripe_ris.withdrawal_prefix_events","routing.ripe_ris.distinct_prefixes_observed","routing.ripe_ris.distinct_announced_prefixes","routing.ripe_ris.distinct_withdrawn_prefixes","routing.ripe_ris.distinct_origin_asns_observed"]as const;function hash(value:unknown):string{return createHash("sha256").update(canonicalJsonStringify(value)).digest("hex");}function strings(value:unknown):string[]{return Array.isArray(value)?value.filter((item):item is string=>typeof item==="string"):[];}function numbers(value:unknown):number[]{return Array.isArray(value)?value.filter((item):item is number=>typeof item==="number"):[];}function expectedMinutes(granularity:MeasurementGranularity):number{return granularity==="ONE_MINUTE"?1:granularity==="FIVE_MINUTES"?5:granularity==="HOUR"?60:1440;}async function activeCalculation(client:PoolClient,key:string):Promise<{definitionId:string;calculationId:string}|null>{const result=await client.query<{active_definition_id:string;active_calculation_id:string}>(`SELECT active_definition_id,active_calculation_id FROM measurement_definition_heads WHERE measurement_key=$1`,[key]);const row=result.rows[0];return row?{definitionId:row.active_definition_id,calculationId:row.active_calculation_id}:null;}
async function materialize(client:PoolClient,input:{sourceDefinitionId:string;granularity:MeasurementGranularity;bucketStart:string;bucketEnd:string}):Promise<void>{const rows=await client.query<{id:string;capture_profile_revision_id:string|null;update_message_count:string;announcement_prefix_event_count:string;withdrawal_prefix_event_count:string;announced_prefixes:unknown;withdrawn_prefixes:unknown;all_prefixes:unknown;origin_asns:unknown;coverage_status:string;data_availability:string}>(`SELECT r.id,r.capture_profile_revision_id,r.update_message_count::text,r.announcement_prefix_event_count::text,r.withdrawal_prefix_event_count::text,r.announced_prefixes,r.withdrawn_prefixes,r.all_prefixes,r.origin_asns,r.coverage_status,r.data_availability FROM routing_minute_bucket_heads h JOIN routing_minute_bucket_revisions r ON r.id=h.current_revision_id WHERE h.source_definition_id=$1 AND h.bucket_start>=$2 AND h.bucket_start<$3 ORDER BY h.bucket_start`,[input.sourceDefinitionId,input.bucketStart,input.bucketEnd]);const expected=expectedMinutes(input.granularity);const profiles=[...new Set(rows.rows.map((row)=>row.capture_profile_revision_id).filter((value):value is string=>Boolean(value)))].sort();const compatibleProfile=profiles.length===1&&rows.rows.every((row)=>row.capture_profile_revision_id===profiles[0]);const complete=rows.rows.length===expected&&rows.rows.every((row)=>row.coverage_status==="COMPLETE"&&row.data_availability==="AVAILABLE")&&compatibleProfile;const degraded=rows.rows.some((row)=>row.coverage_status==="DEGRADED");const coverage=complete?"COMPLETE":rows.rows.length===0?"NO_COVERAGE":degraded?"DEGRADED":"PARTIAL";const availability=complete?"AVAILABLE":rows.rows.length===0?"UNAVAILABLE":"PARTIAL";const updateMessages=rows.rows.reduce((sum,row)=>sum+Number(row.update_message_count),0);const announcements=rows.rows.reduce((sum,row)=>sum+Number(row.announcement_prefix_event_count),0);const withdrawals=rows.rows.reduce((sum,row)=>sum+Number(row.withdrawal_prefix_event_count),0);const allPrefixes=[...new Set(rows.rows.flatMap((row)=>strings(row.all_prefixes)))].sort();const announcedPrefixes=[...new Set(rows.rows.flatMap((row)=>strings(row.announced_prefixes)))].sort();const withdrawnPrefixes=[...new Set(rows.rows.flatMap((row)=>strings(row.withdrawn_prefixes)))].sort();const originAsns=[...new Set(rows.rows.flatMap((row)=>numbers(row.origin_asns)))].sort((a,b)=>a-b);const values:Record<(typeof KEYS)[number],number>={"routing.ripe_ris.update_messages":updateMessages,"routing.ripe_ris.announcement_prefix_events":announcements,"routing.ripe_ris.withdrawal_prefix_events":withdrawals,"routing.ripe_ris.distinct_prefixes_observed":allPrefixes.length,"routing.ripe_ris.distinct_announced_prefixes":announcedPrefixes.length,"routing.ripe_ris.distinct_withdrawn_prefixes":withdrawnPrefixes.length,"routing.ripe_ris.distinct_origin_asns_observed":originAsns.length};const inputFingerprint=hash({revisionIds:rows.rows.map((row)=>row.id),profiles});for(const key of KEYS){if(!getMeasurementRegistration(key))continue;const ids=await activeCalculation(client,key);if(!ids)continue;const current=await client.query<{id:string;revision_number:number;input_fingerprint:string}>(`SELECT r.id,r.revision_number,r.input_fingerprint FROM measurement_bucket_heads h JOIN measurement_bucket_revisions r ON r.id=h.current_revision_id WHERE h.measurement_calculation_id=$1 AND h.granularity=$2 AND h.bucket_start=$3 AND h.scope_key='GLOBAL' FOR UPDATE OF h`,[ids.calculationId,input.granularity,input.bucketStart]);const prior=current.rows[0];if(prior?.input_fingerprint.trim()===inputFingerprint)continue;const numeric=complete?values[key]:null;const inserted=await client.query<{id:string}>(`INSERT INTO measurement_bucket_revisions(measurement_definition_id,measurement_calculation_id,granularity,bucket_start,bucket_end,scope_key,time_axis,value_numeric,coverage_status,expectation_status,data_availability,acquisition_summary,bucket_state,comparison_context,input_fact_count,input_fingerprint,coverage_input_fingerprint,revision_number,supersedes_revision_id,revision_reason) VALUES($1,$2,$3,$4,$5,'GLOBAL','SOURCE_OBSERVED_TIME',$6,$7,'EXPECTED',$8,$9::jsonb,$10,$11::jsonb,$12,$13,$13,$14,$15,$16) RETURNING id`,[ids.definitionId,ids.calculationId,input.granularity,input.bucketStart,input.bucketEnd,numeric,coverage,availability,canonicalJsonStringify([{basis:"RIPE_RIS_STREAM",minuteRows:rows.rows.length}]),prior?'REVISED':'SETTLED',canonicalJsonStringify({captureProfileRevisionIds:profiles,populationCompatible:compatibleProfile}),rows.rows.length,inputFingerprint,(prior?.revision_number??0)+1,prior?.id??null,prior?'ROUTING_INPUT_REVISED':'ROUTING_BUCKET_MATERIALIZED']);const id=inserted.rows[0]?.id;if(!id)throw new Error(`Failed routing measurement ${key}`);await client.query(`INSERT INTO measurement_bucket_heads(measurement_calculation_id,granularity,bucket_start,bucket_end,scope_key,current_revision_id) VALUES($1,$2,$3,$4,'GLOBAL',$5) ON CONFLICT(measurement_calculation_id,granularity,bucket_start,scope_key) DO UPDATE SET bucket_end=EXCLUDED.bucket_end,current_revision_id=EXCLUDED.current_revision_id,updated_at=now()`,[ids.calculationId,input.granularity,input.bucketStart,input.bucketEnd,id]);}const coverageCurrent=await client.query<{id:string;revision_number:number;input_fingerprint:string}>(`SELECT r.id,r.revision_number,r.input_fingerprint FROM source_coverage_bucket_heads h JOIN source_coverage_bucket_revisions r ON r.id=h.current_revision_id WHERE h.source_definition_id=$1 AND h.granularity=$2 AND h.bucket_start=$3 FOR UPDATE OF h`,[input.sourceDefinitionId,input.granularity,input.bucketStart]);const cp=coverageCurrent.rows[0];if(cp?.input_fingerprint.trim()!==inputFingerprint){const satisfied=complete?expected:0;const partial=coverage==='PARTIAL'?Math.min(expected,rows.rows.length):0;const failed=coverage==='DEGRADED'?Math.min(1,expected):0;const missing=Math.max(0,expected-satisfied-partial-failed);const inserted=await client.query<{id:string}>(`INSERT INTO source_coverage_bucket_revisions(source_definition_id,granularity,bucket_start,bucket_end,expectation_status,coverage_status,evaluation_state,expected_opportunity_count,satisfied_opportunity_count,partial_opportunity_count,failed_opportunity_count,missing_opportunity_count,schedule_origin,reason_codes,input_fingerprint,revision_number,supersedes_revision_id) VALUES($1,$2,$3,$4,'EXPECTED',$5,$6,$7,$8,$9,$10,$11,'AUTHORITATIVE_NODE3',$12::jsonb,$13,$14,$15) RETURNING id`,[input.sourceDefinitionId,input.granularity,input.bucketStart,input.bucketEnd,coverage,cp?'REVISED':'FINAL',expected,satisfied,partial,failed,missing,canonicalJsonStringify([compatibleProfile?"STREAM_TELEMETRY_COVERAGE":"STREAM_POPULATION_INCOMPATIBLE"]),inputFingerprint,(cp?.revision_number??0)+1,cp?.id??null]);const id=inserted.rows[0]?.id;if(id)await client.query(`INSERT INTO source_coverage_bucket_heads(source_definition_id,granularity,bucket_start,bucket_end,current_revision_id) VALUES($1,$2,$3,$4,$5) ON CONFLICT(source_definition_id,granularity,bucket_start) DO UPDATE SET bucket_end=EXCLUDED.bucket_end,current_revision_id=EXCLUDED.current_revision_id,updated_at=now()`,[input.sourceDefinitionId,input.granularity,input.bucketStart,input.bucketEnd,id]);}}
export async function routingMeasurementTick():Promise<boolean>{return withTransaction(async(client)=>{const dirty=await client.query<{source_definition_id:string;bucket_start:Date}>(`SELECT source_definition_id,bucket_start FROM routing_measurement_dirty_minutes ORDER BY dirty_since,bucket_start FOR UPDATE SKIP LOCKED LIMIT 1`);const row=dirty.rows[0];if(!row)return false;for(const granularity of["ONE_MINUTE","FIVE_MINUTES","HOUR","DAY"]as const){const bucket=bucketForInstant(row.bucket_start,granularity);await materialize(client,{sourceDefinitionId:row.source_definition_id,granularity,bucketStart:bucket.start,bucketEnd:bucket.end});}await client.query(`DELETE FROM routing_measurement_dirty_minutes WHERE source_definition_id=$1 AND bucket_start=$2`,[row.source_definition_id,row.bucket_start]);return true;});}
